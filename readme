sudo modprobe gs_usb

# bring up can interface
sudo ip link set can0 up type can bitrate 500000

# install can utils
#sudo apt install -y can-utils

ip addr
雷达网段 和 工控机网段

<arg name="hostname" default="192.168.0.1"/>  <!-- IP address of sensor -->
<arg name="udp_receiver_ip" default="192.168.0.100"/>  <!-- UDP destination IP address (ip address of udp receiver) -->


# step:
#1. 设置CAN通信
sudo modprobe gs_usb
sudo ip link set can0 up type can bitrate 500000

#2. 启动底盘
roslaunch ranger_bringup ranger.launch 
